<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{{ title }}</title>
        
    </head>
    <body>
        <header>
            <a href="{{ '/' | url }}" class="link--home">My Blog</a>
            <a href="{{ '/about/' | url }}" class="link--about">About</a>
            <h1>{{ title }}</h1>
        </header>
        <main>
            {{ content | safe }}
        </main>
        <footer>&copy; JM Storm</footer>
    </body>
</html>